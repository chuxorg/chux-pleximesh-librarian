{
  "info": {
    "_postman_id": "817057dc-4635-4b41-a4af-9ddfceb64832",
    "name": "PlexiMesh Librarian Read API",
    "description": "Read-only collection aligned with librarian/openapi/librarian.v0.yaml. Write operations are intentionally excluded.",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
  },
  "item": [
    {
      "name": "GET /artifacts/{artifact_id} \u2014 Latest Active",
      "request": {
        "method": "GET",
        "header": [
          {
            "key": "Accept",
            "value": "application/json"
          }
        ],
        "url": "{{baseUrl}}/artifacts/{{artifactId}}",
        "description": "Returns the latest active version. Deterministic ordering is applied internally when selecting the latest version."
      }
    },
    {
      "name": "GET /artifacts/{artifact_id}?version=vX \u2014 Exact Version",
      "request": {
        "method": "GET",
        "header": [
          {
            "key": "Accept",
            "value": "application/json"
          }
        ],
        "url": "{{baseUrl}}/artifacts/{{artifactId}}?version={{versionParam}}",
        "description": "Exact version lookup (vN)."
      }
    },
    {
      "name": "GET /tasks/{task_id} \u2014 Latest Active",
      "request": {
        "method": "GET",
        "header": [
          {
            "key": "Accept",
            "value": "application/json"
          }
        ],
        "url": "{{baseUrl}}/tasks/{{taskId}}",
        "description": "Typed alias for task artifacts."
      }
    },
    {
      "name": "GET /policies/{policy_id} \u2014 Latest Active",
      "request": {
        "method": "GET",
        "header": [
          {
            "key": "Accept",
            "value": "application/json"
          }
        ],
        "url": "{{baseUrl}}/policies/{{policyId}}",
        "description": "Typed alias for policy artifacts."
      }
    },
    {
      "name": "GET /phases/{phase_id} \u2014 Latest Active",
      "request": {
        "method": "GET",
        "header": [
          {
            "key": "Accept",
            "value": "application/json"
          }
        ],
        "url": "{{baseUrl}}/phases/{{phaseId}}",
        "description": "Typed alias for phase artifacts."
      }
    },
    {
      "name": "GET /artifacts \u2014 List",
      "request": {
        "method": "GET",
        "header": [
          {
            "key": "Accept",
            "value": "application/json"
          }
        ],
        "url": "{{baseUrl}}/artifacts?kind={{kindFilter}}&domain={{domainFilter}}&status={{statusFilter}}&tag={{tagFilter}}",
        "description": "Lists artifacts filtered by kind/domain/status/tag. Results are ordered by (artifact_id, version)."
      }
    },
    {
      "name": "GET /health \u2014 Expected 404",
      "request": {
        "method": "GET",
        "header": [
          {
            "key": "Accept",
            "value": "application/json"
          }
        ],
        "url": "{{baseUrl}}/health",
        "description": "No health endpoint is implemented; expect 404 Unknown path."
      }
    }
  ]
}
